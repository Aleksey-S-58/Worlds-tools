#------------ We will test our application via H2 base -------------
hibernate.dialect=org.hibernate.dialect.H2Dialect
jdbc.driver.name=org.h2.Driver
jdbc.url=jdbc:h2:mem:map;INIT=RUNSCRIPT FROM 'classpath:sql/init.sql'
jdbc.username=sa
jdbc.password=sa